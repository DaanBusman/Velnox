{
  "name": "@velnox/shared",
  "version": "0.5.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Shared contracts, error codes and redaction used by the API, the worker and the web app",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
